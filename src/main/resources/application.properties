# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://volunteer-auth-db.mysql.database.azure.com:3306/auth_db?useSSL=true&serverTimezone=UTC
spring.datasource.username=myadmin@volunteer-auth-db
spring.datasource.password=Achraf@123!2024
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=jwtSecretKey2024@VolunteerApp#SecureKey$Production%Environment
jwt.expiration=86400000

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin

# Logging Configuration
logging.level.com.volunteer=DEBUG
logging.level.org.springframework.security=DEBUG